<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SyncVision" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <!-- Dark mode -->
    <!-- Screen background if Dark mode is on -->
    <style name="SCREEN">
        <item name="android:background">#232323</item>
    </style>

    <!-- Text background if dark mode is on -->
    <style name="TEXT">
        <item name="android:textColor">#ffffff</item>
    </style>

    <!-- Button background if dark mode is on -->

    <style name="BUTTON">
        <item name="android:backgroundTint">#569ABA</item>
        <item name="android:textColor">#E6DBDB</item>
    </style>
</resources>